-- ============================================================
-- ATS: RBAC — крок 1/2 (роль visitor, окрема транзакція)
-- ============================================================
-- Нова системна роль «Відвідувач» (read-only). ALTER TYPE ADD VALUE — окремою
-- міграцією перед тією, що ним користується (seed roles).
-- ============================================================

alter type public.app_role add value if not exists 'visitor';
