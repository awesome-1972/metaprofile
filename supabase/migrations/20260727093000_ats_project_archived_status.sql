-- ============================================================
-- ATS: Архівування проектів — статус 'archived'
-- ============================================================
-- Клієнт уже має client_status='archived'. Для симетрії додаємо 'archived'
-- у hiring_project_status (окрема семантика: archived = приховати з активних
-- списків, не плутати з closed/cancelled, що описують ВИХІД проекту).
-- ALTER TYPE ADD VALUE — окрема міграція (не в tx з використанням значення).
-- ============================================================

alter type public.hiring_project_status add value if not exists 'archived';
