-- ============================================================
-- Другий (тестовий) тенант — для перевірки ізоляції мультитенанта
-- ============================================================
-- Тенант визначається profiles.tenant_id (mp_current_tenant з фолбеком на MetaVision).
-- Identity-таблиці (profiles/user_roles/roles) НЕ гейтовані (міграція 096000), тож
-- вхід не залежить від tenant-контексту — додавання тенанта вхід не ламає.
-- Прив'язка користувача до цього тенанта — окремим SQL (див. docs), бо тестового
-- користувача спершу треба створити в Supabase Auth.
-- ============================================================

insert into public.tenants (id, name, slug)
values ('22222222-2222-2222-2222-222222222222', 'Тестова агенція', 'test-agency')
on conflict (id) do nothing;
